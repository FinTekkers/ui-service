/*
 * This file was generated by the Gradle 'init' task.
 */

plugins {
    id 'java-library'
    id 'maven-publish'
}


repositories {
    mavenLocal()
    maven {
        url = uri('https://repo.maven.apache.org/maven2/')
    }
}

dependencies {
    api 'com.googlecode.json-simple:json-simple:1.1.1'
    api 'com.google.protobuf:protobuf-java:3.21.9'
    api 'com.google.code.gson:gson:2.10'
    api 'com.amazonaws:aws-java-sdk:1.12.353'
    api 'org.apache.commons:commons-lang3:3.12.0'
    api 'org.apache.commons:commons-text:1.10.0'
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.2'
}

task run_number {
    println System.getenv("run_number")
}

group = 'io.github.fintekkers'
version = '1.0.'+ System.getenv("run_number")
description = 'FinTekkers Ledger Models'
java.sourceCompatibility = JavaVersion.VERSION_17


task sourcesJar(type: Jar) {
    duplicatesStrategy = 'include'
    classifier = 'sources'
    from sourceSets.main.allSource
}

java {
    withSourcesJar()
    withJavadocJar()
}

sourceSets {
    main {
        java {
            srcDirs 'src/main/java'
            srcDirs 'ledger-models-java'
        }
    }
}

javadoc {
    failOnError = false
}

publishing {
    repositories {
        maven {
            name = "GitHubPackages"
            url = uri("https://maven.pkg.github.com/fintekkers/ledger-models")
            credentials {
                username = project.findProperty("gpr.user") ?: "dado0583"
                password = project.findProperty("gpr.key") ?: System.getenv("PUBLISH_TOKEN")
            }
        }
    }
    publications {
        gpr(MavenPublication) {
            from(components.java)
        }
    }
}